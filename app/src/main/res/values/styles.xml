<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorWhite</item>
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>

        <item name="actionMenuTextColor">@color/colorGray</item>

    </style>

    <style name="mSearchView" parent="Widget.AppCompat.SearchView">
        <item name="searchIcon">@drawable/ic_action_search</item>
    </style>

    <style name="AppTheme.OverlapSystemBar" parent="AppTheme">
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:fitsSystemWindows">true</item>
    </style>

    <style name="CollapsedTitle">
        <item name="android:fontFamily">@font/cloister</item>
    </style>

    <style name="Toolbar" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />


    <style name="TextNavigationItem">
        <item name="android:textSize">16dp</item>
        <item name="android:color">@color/colorWhite</item>
    </style>

    <style name="menuPopUpTheme">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:textColor">@color/colorWhite</item>
    </style>

    <style name="ShowAlertDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/colorWhite</item>
        <item name="android:background">@color/colorPrimaryDark</item>
        <item name="android:textColorPrimary">@color/colorLight</item>
    </style>

    <style name="ExpandedTitle">
        <item name="android:textSize">18dp</item>
        <item name="android:visibility">invisible</item>
        <item name="android:visible">false</item>
        <item name="android:color">@android:color/transparent</item>
        <item name="android:fontFamily">@font/cloister</item>
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">3</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">wrap_content</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
